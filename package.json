{
  "name": "mubell-farm",
  "version": "1.0.0",
  "description": "MuBell Farm website — static site generator",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && npx serve dist"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
